/**
 * Agent version string baked into every evidence bundle. Bump when the pipeline
 * logic that affects bundle contents changes — verifiers can use it to reason
 * about how a given blob was produced.
 */
export const AGENT_VERSION = "heor-provenance/0.1.0";
